#include<stdio.h>
int main()
{
int number,remainder;
printf("\n Enter the two digits number");
scanf("%d",&numbeer);
original number=number;
{
remainder=original number;
if(result==number)
printf("\n Enter the armstrong number");
else
printf("\n Enter the not armstrong number");
return 0
}
